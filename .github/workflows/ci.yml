name: CI

on:
  push:
    branches: [main, dev, 'feature/**']
  pull_request:
    branches: [main, dev]

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      task-service: ${{ steps.changes.outputs.task-service }}
      project-service: ${{ steps.changes.outputs.project-service }}
      notification-service: ${{ steps.changes.outputs.notification-service }}
      frontend: ${{ steps.changes.outputs.frontend }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - name: Detect changed services
        id: changes
        run: bash .github/scripts/detect-changes.sh

  build-services:
    needs: detect-changes
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - service: task-service
          - service: project-service
          - service: notification-service
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci -w services/${{ matrix.service }}
      - name: npm audit
        run: npm audit --audit-level=moderate -w services/${{ matrix.service }} --omit=dev
        continue-on-error: true
      - name: Lint
        run: npm run lint --if-present -w services/${{ matrix.service }}
      - name: Architecture lint
        run: npm run lint:arch --if-present -w services/${{ matrix.service }}
        continue-on-error: true
      - name: Run tests
        run: npm test -w services/${{ matrix.service }} -- --coverage --coverageReporters=lcov --coverageReporters=json-summary
        env:
          USE_INMEMORY: "true"
          JWT_SECRET: "change-me-in-production"
      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.service }}
          path: services/${{ matrix.service }}/coverage/
      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.service }}
          path: services/${{ matrix.service }}/test-results/
  
  sonarqube:
    needs: [build-services]
    if: always() && !cancelled()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Run tests with coverage
        run: |
          npm test -w services/task-service -- --coverage --coverageReporters=lcov || true
          npm test -w services/project-service -- --coverage --coverageReporters=lcov || true
        env:
          USE_INMEMORY: "true"
          JWT_SECRET: "change-me-in-production"
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        continue-on-error: true

  build-frontend:
    needs: detect-changes
    if: needs.detect-changes.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci -w frontend
      - name: Lint
        run: npm run lint --if-present -w frontend
        continue-on-error: true
      - run: npm run build -w frontend

  docker-build:
    needs: [build-services, build-frontend]
    if: always() && !failure()
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set lowercase repo
        run: echo "REPO=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build task-service image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/task-service/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: ${{ github.ref == 'refs/heads/main' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ghcr.io/${{ env.REPO }}/task-service:latest
            ghcr.io/${{ env.REPO }}/task-service:${{ github.sha }}

      - name: Build project-service image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/project-service/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: ${{ github.ref == 'refs/heads/main' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ghcr.io/${{ env.REPO }}/project-service:latest
            ghcr.io/${{ env.REPO }}/project-service:${{ github.sha }}

      - name: Build notification-service image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/notification-service/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: ${{ github.ref == 'refs/heads/main' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ghcr.io/${{ env.REPO }}/notification-service:latest
            ghcr.io/${{ env.REPO }}/notification-service:${{ github.sha }}

      - name: Build frontend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: frontend/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: ${{ github.ref == 'refs/heads/main' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ghcr.io/${{ env.REPO }}/frontend:latest
            ghcr.io/${{ env.REPO }}/frontend:${{ github.sha }}

      - name: Image URLs summary
        if: github.ref == 'refs/heads/main'
        run: |
          echo "## 🐳 Images publiées sur GHCR" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- \`ghcr.io/${{ env.REPO }}/task-service:${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- \`ghcr.io/${{ env.REPO }}/project-service:${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- \`ghcr.io/${{ env.REPO }}/notification-service:${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- \`ghcr.io/${{ env.REPO }}/frontend:${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY

  hadolint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lint task-service Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: services/task-service/Dockerfile
      - name: Lint project-service Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: services/project-service/Dockerfile
      - name: Lint notification-service Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: services/notification-service/Dockerfile
      - name: Lint frontend Dockerfile
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: frontend/Dockerfile

  trivy:
    needs: docker-build
    if: always() && !failure()
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Trivy scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: .
          severity: CRITICAL,HIGH
          format: sarif
          output: trivy-results.sarif
      - name: Upload Trivy results
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: trivy-results.sarif
        continue-on-error: true

  validate-compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate docker-compose
        run: docker compose config -q

  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Gitleaks secret scan
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  license-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Check licenses task-service
        run: npx license-checker --production --failOn "GPL-3.0;AGPL-3.0" --start services/task-service
        continue-on-error: true
      - name: Check licenses project-service
        run: npx license-checker --production --failOn "GPL-3.0;AGPL-3.0" --start services/project-service
        continue-on-error: true
      - name: Check licenses notification-service
        run: npx license-checker --production --failOn "GPL-3.0;AGPL-3.0" --start services/notification-service
        continue-on-error: true
      - name: Check licenses frontend
        run: npx license-checker --production --failOn "GPL-3.0;AGPL-3.0" --start frontend
        continue-on-error: true

  pipeline-summary:
    needs: [build-services, build-frontend, docker-build, hadolint, trivy, validate-compose, gitleaks, license-check, sonarqube]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Pipeline Summary
        run: |
          echo "## 🚀 Pipeline CI — Résumé" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| build-services | ${{ needs.build-services.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| build-frontend | ${{ needs.build-frontend.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| docker-build | ${{ needs.docker-build.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| hadolint | ${{ needs.hadolint.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| trivy | ${{ needs.trivy.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| validate-compose | ${{ needs.validate-compose.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| gitleaks | ${{ needs.gitleaks.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| license-check | ${{ needs.license-check.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| sonarqube | ${{ needs.sonarqube.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY